class Solution {
    public String restoreString(String s, int[] indices) {
        char[] retStr = new char[indices.length];
        for(int i = 0 ; i < indices.length ; i++) {
            retStr[indices[i]] = s.charAt(i);
        }
String str = String.copyValueOf(retStr);
return str;
    }
}
